language: generic
before_install:
  - sudo apt-get -qq update
  - sudo apt-get install -y --no-install-recommends texlive-latex-base latex-beamer

script:
  - mkdir build && cd build
  - cmake ..
  - make
